<% js = escape_javascript(
  render(partial: 'jobs/list', locals: { jobs: @filtered_jobs })
) %>
$("#filterrific_results").html("<%= js %>");

$('#filtered_job').html("<%= escape_javascript(render(partial: 'jobs/filter', locals: { jobs: @filtered_jobs }) ) %>");
